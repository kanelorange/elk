server.host: "0.0.0.0"
elasticsearch.hosts: ["http://${HOSTNAME_ELASTIC}:9200"]
elasticsearch.username: "${ELASTIC_USERNAME}"
elasticsearch.password: "${ELASTIC_PASSWORD}"
xpack.monitoring.ui.container.elasticsearch.enabled: true